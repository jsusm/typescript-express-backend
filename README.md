## Start the project
```bash
# clone the repository
git clone https://github.com/jsusm/typescript-express-bakend
cd typescript-express-backend

# Install dependencies
npm install

# start the postgres database
docker compose up postgres -d

# start the project
npm start
```
